apply plugin: 'java'

ext {
  projectName = 'iks-core'
}

dependencies {
  compile project(':iks-common')
  compile 'ch.qos.logback:logback-classic:1.1.3'
}

sourceSets {
  main {
    java {
      srcDir 'src'
    }
  }
}