include "../gul/gul-schema.rnc"

gul.element |= appTable | referenceTableField

appObjAttr = attribute appObj { xsd:NCName }

appTable = element appTable {
  idAttr
  & appObjAttr
  & filterPanel?
  & columns
}

filterPanel = element filterPanel {
  gul.element*
}

column = element column {
  fieldNameAttr?
  & labelAttr?
  & attribute displayField { text }?
}+

columns = element columns {
  column+
}

referenceTableField = element referenceTableField {
  fieldNameAttr?
  & nameAttr?
  & labelAttr?
  & columns
}