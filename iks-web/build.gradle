apply plugin: 'java'

ext {
  projectName = 'iks-web'
}

dependencies {
  compile project(':iks-core')

  compile 'org.springframework:spring-webmvc:4.1.6.RELEASE'
}

sourceSets {
  main {
    java {
      srcDir 'src/main/java'
    }
  }
}

